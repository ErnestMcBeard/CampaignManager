<Page
    x:Class="CampaignManager.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:modules="using:CampaignManager.Views.HomePageModules"
    DataContext="{Binding HomePageViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Image Source="ms-appx:///Assets/HeaderImage.jpg"
                Stretch="UniformToFill"
                MaxHeight="400"
                Grid.ColumnSpan="2" />
        <Pivot Grid.Row="1"
                Grid.RowSpan="2"
                Grid.ColumnSpan="2">
            <PivotItem Header="Campaigns">
                <RelativePanel>
                    <Button x:Name="AddCampaignButton"
<<<<<<< HEAD
                            Background="Transparent"
                            RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignTopWithPanel="True"
                            Style="{StaticResource TransparentButton}">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE710;"
                                   Style="{StaticResource TransparentButton}" />
                    </Button>
                </RelativePanel>

            </PivotItem>
            <PivotItem Header="Characters">
                <TextBlock Text="List of Player Characters"
                           Style="{ThemeResource HeaderTextBlockStyle}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

            </PivotItem>
            <PivotItem Header="Monsters">
                    
            </PivotItem>
            <PivotItem Header="Items">
                    
            </PivotItem>
            <PivotItem Header="Spells">
                    
=======
                            Style="{StaticResource TransparentButton}"
                            RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignTopWithPanel="True"
                            Click="AddCampaignButton_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                    Text="&#xE710;"
                                    FontSize="24" />
                    </Button>
                </RelativePanel>
            </PivotItem>
            <PivotItem Header="Characters">
                <RelativePanel>
                    <Button x:Name="AddCharacterButton"
                            Style="{ThemeResource TransparentButton}"
                            RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignTopWithPanel="True"
                            Click="AddCharacterButton_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE710;"
                                   FontSize="24" />
                    </Button>
                </RelativePanel>
            </PivotItem>
            <PivotItem Header="Monsters">
                <RelativePanel>
                    <Button x:Name="AddMonsterButton"
                            Style="{ThemeResource TransparentButton}"
                            RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignTopWithPanel="True"
                            Click="AddMonsterButton_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE710;"
                                   FontSize="24" />
                    </Button>
                </RelativePanel>
            </PivotItem>
            <PivotItem Header="Items">
                <RelativePanel>
                    <Button x:Name="AddItemButton"
                            Style="{ThemeResource TransparentButton}"
                            RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignTopWithPanel="True"
                            Click="AddItemButton_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE710;"
                                   FontSize="24" />
                    </Button>
                    <Grid RelativePanel.Below="AddItemButton"
                          RelativePanel.AlignLeftWithPanel="True"
                          RelativePanel.AlignRightWithPanel="True"
                          RelativePanel.AlignBottomWithPanel="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition MinWidth="300"/>
                            <ColumnDefinition Width="4*" />
                        </Grid.ColumnDefinitions>
                        <ListView ItemsSource="{Binding Items}" />
                    </Grid>
                </RelativePanel>
            </PivotItem>
            <PivotItem Header="Spells">
                <RelativePanel>
                    <Button x:Name="AddSpellButton"
                            Style="{ThemeResource TransparentButton}"
                            RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignTopWithPanel="True"
                            Click="AddSpellButton_Click">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE710;"
                                   FontSize="24" />
                    </Button>
                    <Grid RelativePanel.Below="AddSpellButton"
                          RelativePanel.AlignLeftWithPanel="True"
                          RelativePanel.AlignRightWithPanel="True"
                          RelativePanel.AlignBottomWithPanel="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition MinWidth="300" />
                            <ColumnDefinition Width="4*" />
                        </Grid.ColumnDefinitions>
                        <ListView ItemsSource="{Binding Spells}" />
                    </Grid>
                </RelativePanel>
>>>>>>> 176e4655528fac6ca261b94513c59174179c075b
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
